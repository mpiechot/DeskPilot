# Extract BrowserPilot Before Generalizing Shared Pilot Concepts

DeskPilot will first encapsulate the complete existing browser-session product as BrowserPilot without changing its proven behavior or data ownership. Only after another Pilot exists for comparison will shared concepts be generalized; this keeps the first shell migration small while still preserving a deliberate follow-up for useful cross-Pilot abstractions.
